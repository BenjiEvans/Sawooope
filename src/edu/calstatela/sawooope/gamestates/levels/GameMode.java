package edu.calstatela.sawooope.gamestates.levels;

/**
 * Instead of creating a GameMode Class we can use the enum constants 
 * to differentiate the game mode that the level is using
 * @author Benji
 *
 */
public enum GameMode {

	TIME,SURVIVAL,ESCAPE
}
